#include <iostream>
#include <fstream>		// ???? ??????? ????? ??? ?????? ????????? ???. 
using namespace std;

int main()
{
	ifstream is;
	is.open("Source/15장/score.txt");
	if( !is ) {		// ! ?????? ?????ε?
		cerr <<"파일 오픈에 실패하였습니다"<< endl;
		exit( 1 );
	}
	char c;
	while(! is.eof() )
	{
		is.get(c); 
		cout << c;
	}
	is.close();
	return 0;
}
