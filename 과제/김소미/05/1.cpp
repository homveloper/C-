// 연습문제 1. 주어진 정수가 소수(prime number)인지 검사하는 함수 isPrime()을 작성하라.
//이 함수를 이용하여 1 부터 100 사이에 존재하는 소수들을 모두 출력하라.

#include <iostream>
#include <cmath>
using namespace std;
int isPrime(int n, int i);

int main() {
	int n;
	int i = n;
	cin >> n;
}

// 입력 : 정수?
// 출력 : true or false

int isPrime(int n) {
	int result  = n % i;

	for (int i == 1; i < n; i++) {
		if (rest = 0)
			cout << "소수가 아닙니다." << endl;

		return n % (i - 1);
	}
}

