#include <iostream>
using namespace std;

// 1. s1.append(s2) : s1의 마지막 위치에 s2를 추가한다.
// 2. s1.compare(s2) : s2의 값을 인수로 지정된 문자와 비교한다.
// 3. s1.find('A',0) : 문자 'A'를 검색한다. 문자열의 0번째 요소부터 검색.
// 4. s1.at(2) : 문자열에서 2번째에 있는 문자에 대한 참조를 반환한다.
// 5. s1.clear() : 문자열의 내용을 지워 빈문자열이 되게 한다.
int main() {
	cout << "Hellow world" << endl;

}